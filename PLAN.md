# 온라인테스트1 진행 현황

마지막 업데이트: 2026-07-11 (soil_ec 뭉침 문제 2가지 수정 시도 — 둘 다 실패, train.py 원상유지로 최종 종결)

```
온라인테스트1 (생육환경 예측)
│
├─ 1. 센서(env) EDA [01_eda.py]                         ✅ 완료
│   ├─ 구조·결측치·이상치 확인                            ✅
│   ├─ train/test 분포 차이 파악                          ✅
│   │   └─ roof_vent1(30→59), wind_speed(6.5→8.3), 내부온도(1.5→-0.2)
│   └─ dt(시간) 인덱스 활성화, 중복 제거                   ✅
│
├─ 2. 다분광(ms) EDA [02_ms_eda.py]                      ✅ 완료
│   ├─ 폴더 구조·촬영 스펙 전수 검증                       ✅
│   │   └─ 위치 0/1/2/3 존재, 촬영 스펙 100% 동일 확인
│   ├─ 5분 그리드 시간 매칭 (forward-fill, 3시간 제한)      ✅
│   ├─ 이미지→수치 피처 변환 (밴드별 평균)                  ✅
│   ├─ 화질 점검 (밝기/시간대 영향, train+test 둘 다)        ✅
│   │   └─ test 이미지가 train보다 28% 밝음 (train 평균129 vs test 165)
│   │       → solar_radiation 센서값(test가 더 높음)과 교차검증됨
│   │   └─ 최대값 1023 = 10비트 센서 추정, train/test 둘 다 동일
│   ├─ 활용 가치 검증 (day_num 대조실험)                    ✅
│   │   └─ 이미지 신호가 단순 날짜효과 아님을 확인
│   ├─ "9. 화질점검" 중복 블록 정리 완료                     ✅
│   ├─ ⚠️ 위치0 상관관계(0.30~0.37), 표본 크기(n) 확인 안 됨  🔲 미완료 (🟢)
│   │      → 7일치뿐이라 우연한 상관일 가능성 배제 안 됨
│   ├─ ⚠️ avg_band/std_band 결측률 미확인                    🔲 미완료 (🟢)
│   │      → std_band는 위치 2개 이상 동시 존재해야 계산되는데
│   │        위치별 결측률 70~92%라 대부분 NaN일 가능성 높음
│   ├─ ⚠️ 포화(1023) 픽셀 비율 — 발견만 하고 실제로 안 셈    🔲 미완료 (🟢)
│   └─ 🆕 이미지 마스킹 파이프라인 (3인 전문가 검토 반영, 26.07.11)   🔲 계획만 됨, 미착수
│          기존 계획: 마스크 생성 → 마스크 기준 밴드 평균 재계산 → 재검증
│          검토 후 추가된 항목:
│          1) 적응적(상대) 임계값 — 사진마다 절대 밝기 대신 그 사진 내 상대 기준으로 마스킹
│             (train 평균밝기 129 vs test 165, 시간대별로도 71~197로 최대 3배 차이나서 고정 임계값 위험)
│          2) 위치0 별도 검증 — 위치1/2/3용 마스킹 로직이 위치0(넓고 흐릿한 고정카메라, 배경 비중 큼)에도
│             통하는지 따로 확인. 안 통하면 위치0은 다른 마스킹 방식 필요
│          3) 기준판 정체 검증 — 3단계(캘리브레이션 보정) 착수 전에, 사진 속 사각형 물체가 실제
│             반사 기준판인지 먼저 확인. 검증법: 촬영 시간대(조명 세기)에 따라 그 물체의 관측값이
│             같이 변하는지 확인 (진짜 기준판이면 조명 따라 값도 같이 변하는 게 정상)
│          ⛔ 구조적 한계(해결 불가, 문서화만): soil 센서-카메라 위치 대응관계 불명 —
│             위치 좌표 데이터가 아예 없어서(hdr에도 촬영 스펙만 있음) 위치0 상관관계가
│             "센서와 가까워서"인지 "촬영 패턴 차이"인지 구분 불가능. 나중에 이미지 피처가
│             예상보다 약하게 나오면 이 한계 때문일 수 있음을 기억할 것
│   └─ 🔴 불량 데이터 재활용 정책 반영 (실무자 조언, 26.07.11)   ⚠️ 부분 완료 (패널보정만 보류)
│          방향 전환: "이상하면 제외"가 아니라 "이상하면 고쳐서/그대로 살릴 수 있는지 먼저 본다"
│          1) ✅ 밝기 이상치 사진(train 상/하위 1%, test loc0 아침) 실제 확인 완료 —
│             셋 다 손상 아님, min-max 정규화하면 잎 형태 살아남 (저조도=노이즈만 증가, 손상 아님)
│             → 제외 없이 정규화로 재활용 결론
│          2) ✅ 기준판 정체 검증 완료 — 진짜 반사 기준판 맞음으로 확인.
│             프레임 테두리 제외한 패널면만 잘라 loc3 39장(하루 전체 시간대) 비교 결과
│             panel_mean vs whole_image_mean 상관계수 0.958 — 조명 따라 같이 변함(정상 반사판 거동)
│          ⚠️ 2-1) 패널 기반 보정 실제 적용 — 🔲 보류 (26.07.11 시도, 자동탐지 신뢰도 부족)
│             문제: 패널이 세션마다(같은 위치even) 화면 내 위치가 달라서 고정 좌표 박스 사용 불가.
│             numpy만으로 자동탐지(어두운 사각영역 탐지) 프로토타입 시도 → 5개 샘플 중 1개만 정확,
│             나머지는 그림자/틈을 오탐지하거나 못 찾음. scipy(연결요소 분석) 있으면 개선 가능하나
│             이 mac이 Homebrew 관리 시스템 python이라 설치 시 시스템 건드려야 함 → 허락 없이 설치 안 함.
│             → 결정: 지금은 보류. 베이스라인 모델을 막는 항목이 아니므로 우선순위 낮춤.
│             → 재시도 조건: scipy 설치 허락 받거나, 다른 방식(예: 수동으로 세션별 위치 라벨링) 필요
│          3) 위치0(표본 7일, 결측 91%) — 위치 자체를 빼지 않고, "이 시점 위치0 촬영 존재 여부"를
│             0/1 플래그 피처로 추가해 모델이 신뢰도를 알아서 학습하게 함
│          4) std_band 결측(70~92%) — 컬럼 유지, NaN 그대로 LightGBM 등 트리 모델에 맡김
│             (평균 대치 금지 — 이미 결정된 정책, 재활용 원칙과 일치)
│          5) 진짜 손상 파일만 제외 — check_raw_size 기준 hdr/raw 용량 불일치 파일 (현재 0건 확인됨)
│
├─ 2b. EDA 보완 검토 (참고문헌/유튜브 서칭 + 시각화)         ⏳ 진행중 (목표: 07-12)
│   ├─ env(정형) EDA에서 놓친 부분 없는지 참고문헌/영상 서칭    🔲 진행중
│   ├─ ms(이미지) EDA에서 놓친 부분 없는지 참고문헌/영상 서칭    🔲 진행중
│   └─ 지금까지 분석 결과 시각화 진행                          🔲 진행중
│
├─ 3. 전처리 파이프라인 [preprocess.py]                    ✅ 완료 및 검증됨
│   ├─ dt/actuator매핑/day_num,hour 함수화                  ✅
│   ├─ 1분→5분 그리드 집계 (mean/max/min/std/last)          ✅ 인과관계 방향 수정 완료
│   │      resample 결과를 5분 뒤로 shift + reindex 처리:
│   │      라벨 T의 피처가 (T-5,T] 구간(=T 이전 5분)만 담도록 수정
│   │      → y(T) 예측 시점에 미래 데이터가 안 섞이게 고침
│   │      맨 첫 행은 "이전 5분" 데이터가 없어 NaN (정상, 버그 아님)
│   ├─ CLIP_RANGES에 내부습도(humidity) 추가 완료             ✅
│   ├─ (선택) ms 이미지 피처 병합 함수                       ✅ 실행 검증 완료
│   │      build_features(..., ms_matched_path=...) 정상 작동,
│   │      dtype 에러 없음, shape (7488,152) 확인
│   ├─ Train/Val 시간순 분할 (마지막 4일)                    ✅
│   ├─ __main__ 검증 코드 실제 실행                          ✅ 완료
│   │      train_feat (7488,92) / train_y (7488,3) / 인덱스 일치 True
│   ├─ test_X에 대해서도 build_features 검증                 ✅ 완료
│   │      test_feat (3456,92) 정확히 일치 확인
│   └─ (참고자료의 "8개월 스팬"은 오류 — 실제론 38일로 정정)  ✅
│
├─ [requirements.txt / Dockerfile / .dockerignore]         ✅ 완료
│   ├─ pandas/numpy/matplotlib/seaborn 버전 고정            ✅
│   ├─ .dockerignore로 .venv/dataset/.env/캐시 제외          ✅
│   ├─ python:3.11-slim → 3.13-slim (numpy 2.5.1 호환)      ✅
│   ├─ 실제 docker build/run 성공 확인 (hello-world 기준)    ✅
│   └─ dataset/ vs input/ 경로 규칙 (26.07.11 정정)          ✅
│         원래는 "input/dataset/=대회 샘플(1일치, P1_ 위치명)"으로 알고 있었으나,
│         실제로 들어있던 데이터는 전체 실데이터(38일치, 실제 위치명 1/2/3/0)였음.
│         이 데이터를 dataset/(로컬 전체 실데이터 자리)로 옮겨서 사용 중.
│         input/dataset/은 현재 비어있음 — Docker 채점 시 그라더가 자기 데이터를 이 경로에
│         마운트하는 구조로 이해됨. train.py/inference.py는 지금 dataset/ 기준으로 개발 중이며,
│         Docker 제출 직전에 input/dataset/... 경로로 바꿔야 함(4/5단계 체크리스트에 남아있음)
│
├─ [폴더 정리]                                             ✅ 완료
│   ├─ eda_outputs/ — EDA 시각화 PNG 9개                    ✅
│   └─ cache/ — ms 피처/매칭 결과 pkl 6개                   ✅
│
├─ [버전 관리]                                             ✅ 완료
│   ├─ git init 완료, .gitignore 작성, 첫 커밋 완료           ✅
│   └─ GitHub 업로드 — 본인이 수기로 진행 예정                🔲 대기
│
├─ 4. 베이스라인 모델 [train.py]                           ✅ 1차 완료 (26.07.11), 반복 개선 예정
│   ├─ 모델 라이브러리 결정 — LightGBM (타깃별 3개 회귀모델)   ✅
│   │      .venv 새로 만들어서 lightgbm 4.6.0 / scikit-learn 1.9.0 설치, requirements.txt 반영
│   ├─ random_state=42 전체 고정                            ✅
│   ├─ env(정형) 피처만으로 1차 학습 — time_based_split(val 4일), early stopping 적용  ✅
│   │      1차 val RMSE(day_num 포함): soil_moisture 1.0122 / soil_ec 0.0337 / soil_temp 1.7206
│   ├─ 🔴 3인 전문가 비판적 검토 → day_num 외삽 위험 발견 및 대응 (26.07.11)          ✅
│   │      train(DAT109~134)과 test(DAT135~146) 날짜 구간이 아예 안 겹침 — day_num이
│   │      3개 타깃 전부에서 feature importance 1위인데, 트리 모델은 학습 범위 밖으로 외삽을 못 함.
│   │      1차: 폴드 1개짜리 "갭 큰" 실험 → day_num 전체 제거로 임시 결정 (soil_moisture 1.36/soil_ec 0.59/soil_temp 1.25)
│   │      2차: 4-fold 시계열 교차검증(expanding window)으로 재검증 → 1차 결론 일부 뒤집힘.
│   │        1차 실험은 "갭 크기 효과"와 "학습데이터 부족 효과"가 섞여있던 왜곡된 비교였음.
│   │        4-fold 평균: soil_moisture day_num有 1.17±0.40 vs 無 1.63±0.53 (있는 게 나음)
│   │                     soil_ec        day_num有 0.31±0.26 vs 無 0.62±0.10 (있는 게 나음)
│   │                     soil_temp      day_num有 1.42±0.45 vs 無 1.14±0.24 (없는 게 나음, 1차와 동일 결론)
│   │      → 최종 결정(타깃별 차등 적용): soil_moisture/soil_ec는 day_num 유지, soil_temp만 제외.
│   │        train.py의 DROP_COLS_PER_TARGET에 반영, 공식 val RMSE: 1.0122 / 0.0337 / 1.2489
│   │      교훈: 폴드 1개짜리 검증은 성급한 결론으로 이어질 수 있음 — 이후에도 중요한 피처 결정은
│   │        가능하면 다중 폴드로 재확인할 것
│   ├─ input/dataset/... 경로로 읽도록 작성                 🔲 미착수 (Docker 제출 직전 전환)
│   └─ ✅ 다분광(ms) 피처 실제 반영 여부 결정 (26.07.11) — 미반영으로 확정
│          env-only val RMSE(1.3618/0.5936/1.2489) vs env+ms(1.3644/0.5922/1.2709) 비교 결과
│          3개 타깃 다 의미있는 개선 없음(soil_temp는 오히려 소폭 악화) → 이미지 피처 빼고 진행
│          이유 추정: 마스킹(배경 제거)/밝기 보정 둘 다 아직 안 된 날것 피처라 노이즈가 신호를 덮음
│          → 나중에 마스킹·보정을 실제로 구현하면 재시도 가치 있음(지금은 우선순위 낮춤)
│
├─ 5. 추론·제출 [inference.py]                             ✅ 1차 구현 완료 (26.07.11)
│   ├─ model/model.pkl(타깃별 3모델) 로드 → test_feat 예측 → output/submission.csv     ✅
│   │      컬럼명 "timestamp"→"time"으로 수정 (README 스펙: "time 컬럼 기준 매칭"과 일치시킴)
│   ├─ 실제 test(3456행) 예측해서 shape/포맷 확인 완료                                ✅
│   └─ 🚨 발견한 문제: soil_ec 예측이 실제 test에서 심하게 뭉침 (미해결, 알려진 한계로 기록)
│          고유값 104/3456개뿐, 최다값이 39%(1341행) 차지, 같은 값이 12일 전체에 걸쳐 반복됨.
│          train_y의 실제 soil_ec는 하루 288개가 거의 다 고유값(연속적 변화)이라 이 정도 뭉침은
│          비정상. day_num을 빼고 재확인해도 여전히 뭉침(고유값 28개) → day_num이 원인이 아니라
│          더 근본적인 일반화 문제로 추정(원인 미상). soil_moisture/soil_temp는 이 문제 없음(정상).
│          → 지금은 원인 규명 보류: 실제 정답이 없어 어떤 수정이 도움되는지 검증 불가능하고,
│            잘못 건드리면 val 점수만 보고 오히려 악화시킬 위험. 알려진 한계로만 문서화.
│          → (26.07.11 후속) 원인 추가 조사 완료: soil_ec 모델 상위 15개 피처 중 10개가
│            test에서 train 범위를 벗어남(day_num뿐 아니라 temperature_outside/mean,
│            humidity_outside/mean 등 온습도 계열 대부분). day_num 하나만의 문제가 아니라
│            train/test 계절 차이(test가 더 춥고 습함)로 인한 구조적/광범위 분포 이동으로 추정.
│            단일 컬럼 제거로 해결될 문제가 아니라서, 정답 없이 더 건드리는 건 보류 —
│            사용자 판단 필요(다른 작업 우선 진행 vs 이 문제 계속 파기)
│          → (26.07.11 재시도, 둘 다 실패, train.py는 원상유지)
│            시도1) 선형회귀+LightGBM 잔차보정 하이브리드 — 실패. soil_ec가 관수/시비 이벤트성
│              비선형 신호라 선형모델 자체가 부적합(val RMSE 0.48, 기존 0.03보다 훨씬 나쁨).
│              게다가 test 예측에 음수 EC(-0.036, 물리적으로 불가능한 값) 발생.
│            시도2) LightGBM을 더 보수적으로(num_leaves 축소) — 예상과 반대로 더 악화.
│              leaves를 줄이면 뭉뚱그려짐이 심해져서 test 예측이 극단적으로 값 1개로 붕괴됨.
│            → 결론: 지금 방법으로는 해결 못 함. 원래 기본 설정(val 0.0337, 고유값 104개,
│              값 자체는 물리적으로 타당한 범위)이 시도한 것 중 가장 안전 → train.py 변경 없음.
│              "시도했지만 못 고친 알려진 한계"로 최종 기록, 추가 실험은 정답 확보 전까지 보류
│   └─ submission.csv 포맷 assert 검증 (컬럼/행개수/NaN 체크)              🔲 미착수
│
└─ 6. Docker 재현 검증                                     ⚠️ 1차 완료 (hello-world 기준일 뿐)
    └─ 4단계 실제 모델 완성 후 재검증 필수                   🔲 대기
```

## 우선순위 액션 리스트

**✅ 완료 (2026-07-11)**
- 불량 데이터 재활용 정책 적용: cache/eda_outputs 재생성, 밝기 이상치 사진(상하위 1%) 실제 확인(전부 정상),
  기준판이 진짜 보정용 반사판인지 검증(상관계수 0.958로 확인)
- 패널 기반 보정 실제 적용은 자동탐지 신뢰도 부족으로 보류 (위 조직도 2-1 항목 참고)

**🔲 보류 (재시도 조건 충족 시)**
- 패널 기반 밝기 보정 — scipy 설치 허락 또는 세션별 수동 라벨링 필요
- 위치0 존재 플래그 피처 설계 (베이스라인 모델 착수 시 같이 처리 가능)
- CNN(딥러닝) 이미지 모델 검토 (26.07.11, 실무자 제안) — 지금은 우선순위 낮음으로 판단.
  이유: 사진 904장뿐(딥러닝엔 적음) + 결측/마스킹 미비/위치 어긋남 문제 있음 + 이미 단순 평균 밴드
  피처로도 성능 개선 0이었음 → 병목이 "피처 추출 방법의 단순함"이 아니라 "데이터 자체의 정제 부족"
  으로 추정되어, 더 복잡한 모델(CNN)을 얹어도 별 도움 안 될 가능성 높음.
  → 재검토 조건: 마스킹/밝기보정이 실제로 되고 나면, 직접 학습 대신 "사전학습 CNN으로 임베딩만
  추출" 같은 가벼운 방식부터 시도해볼 가치 있음

**🔴 진행중 (2026-07-11 ~ 07-12 목표)**
- env(정형)·ms(이미지) EDA에서 놓친 부분 없는지 참고문헌/유튜브 서칭으로 크로스체크
- 지금까지 분석 결과 시각화 진행

**✅ 완료 (2026-07-10)**
- 🔴 preprocess.py 검증(train+test), 리샘플 인과관계 버그 수정, dataset/input 경로 확정, git 초기화
- 🟡 merge_image_features 검증, CLIP_RANGES humidity 추가, 02_ms_eda.py 중복 제거, test_quality 분석

**🟢 이미지 피처를 실제로 모델에 쓰기로 결정했을 때만 처리**
1. 위치0 상관관계 표본크기(n)/통계적 유의성 재검증
2. std_band 결측률 확인
3. 포화(1023) 픽셀 비율 확인
4. (26.07.11 추가) 이미지 마스킹 파이프라인: 마스크 생성 → 마스크 기준 밴드 평균 재계산 → 재검증
   - 적응적(상대) 임계값 사용 (사진마다 절대 밝기 대신 상대 기준)
   - 위치0은 마스킹 로직이 통하는지 별도 검증
   - 기준판 정체 검증 (3단계 보정 착수 전 필수 — 조명 따라 값이 같이 변하는지 확인)

**⛔ 알려진 구조적 한계 (개선 불가, 참고용으로만 문서화)**
- soil 센서-카메라 위치 대응관계 불명 — 위치 좌표 데이터 자체가 없어(hdr에 촬영 스펙만 존재), 위치0 상관관계가 센서 근접성 때문인지 촬영 패턴 차이인지 구분 불가능

**다음 큰 단계**: train.py/inference.py 1차 완료 (val RMSE: 1.0122/0.0337/1.2489). 다만 soil_ec의
실제 test 예측 뭉침 문제가 미해결 상태 — 시간 되면 feature별 제거 실험으로 원인 추적 필요.
그 외: 하이퍼파라미터 튜닝, submission.csv 포맷 assert 검증, Docker 재현 검증(6단계) 순서로 진행 가능
— 앞으로 중요한 결정은 폴드 1개가 아니라 4-fold + 실제 test 예측 다양성 체크를 같이 봄

## 참고
- 최종 제출 형식은 train_y.csv와 동일 (5분 간격 timestamp,soil_moisture,soil_ec,soil_temp)
- 평가: 항목별 raw RMSE → 순위 점수 변환


#---정리 완료 (26.07.11)---
3명 전문가 검토(A: 다분광 영상처리, B: 통계/검증, C: 온실 도메인) 내용은 위 조직도의
"2. 다분광(ms) EDA" 섹션과 "🟢 이미지 피처를 실제로 모델에 쓰기로 결정했을 때만 처리" 항목에
반영 완료. 원본 논의 기록은 삭제하고 결론만 남김.